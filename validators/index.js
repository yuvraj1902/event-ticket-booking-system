const userValidator=require('./user.validator')
const eventValidator=require('./event.validator')
const showValidator=require('./show.validator')
const addressValidator=require('./address.validator')
const theatreValidator=require('./theatre.validator')
module.exports={
    userValidator,eventValidator,showValidator,addressValidator,theatreValidator
}
